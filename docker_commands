create in the root folder Dockerfile adn docker-compose.yml 

Build the Docker containers:
docker-compose build
Start the containers:
docker-compose up -d
App
http://localhost:8000
phpMyAdmin
http://localhost:8080


docker build -t symfony6 .
docker run -d -p 8080:80 --name symfony symfony6
